version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "Prvi_Task:4"
        ServiceName: "Flask-service"
        ClusterName: "PrviCluster"
